services:
  - type: web
    name: bg-remover-studio
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn bg_remover_server:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: REMOVE_BG_API_KEY
        sync: false
